# CircuitSimDevice 工程目录结构说明

## 1. 文档目的

本文档说明 `CircuitSimDevice` 仓库当前的目录组织方式、各目录的职责，以及哪些内容应纳入版本管理。新建 Vivado/FPGA 工程时，可以参照本文档建立相同的目录骨架。

该结构的核心原则是：

- 将可长期维护的源码、约束、脚本、文档和 IP 与 Vivado 构建产物分开存放。
- Git 仓库只保存无法轻易重建或需要长期复用的内容。
- Vivado、Vitis 和 XSim 产生的工程文件、缓存、日志与编译结果统一放在 `fpga/work/`。
- Vivado 工程应能通过 `fpga/scripts/` 中的 Tcl 脚本重新创建，避免依赖某台计算机上的临时工程状态。

## 2. 当前目录结构

```text
CircuitSimDevice/
├── fpga/                       FPGA 设计与实现相关内容
│   ├── doc/                    FPGA 工程文档和参考资料
│   │   └── image/              文档引用的图片资源
│   ├── scripts/                工程创建、IP 生成、仿真和综合 Tcl/Python 脚本
│   ├── src/                    纳入版本管理的 FPGA 设计源文件
│   │   ├── constrs/            XDC 管脚、时钟和时序约束
│   │   ├── hdl/                可综合 HDL/RTL 源码及公共头文件
│   │   ├── ip/                 需要长期保存和复用的 IP 核文件
│   │   │   ├── coef_bram/      系数 BRAM IP 及其初始化数据
│   │   │   └── dds/            DDS IP 及其配置和配套文件
│   │   ├── sim/                Testbench、仿真模型和仿真检查模块
│   │   └── testmodule/         面向板级测试的顶层设计模块
│   ├── vitis/                  Vitis 软件、平台及处理器端工程源码
│   └── work/                   Vivado/Vitis/XSim 本机构建工作区
├── matlab/                     系统算法、数据生成、建模和验证代码
│   ├── arm/                    计划运行在 ARM/PS 端的算法参考实现
│   ├── data_generator/         系数、激励及初始化数据生成脚本
│   │   └── data/               生成或保存的算法数据和 IP 初始化文件
│   ├── models/                 ADC、DAC、电路等系统级模型
│   ├── pl/                     PL 侧算法的 MATLAB 参考实现
│   ├── results/                MATLAB 运行结果目录
│   └── verification/           仿真结果分析和算法一致性验证脚本
├── .gitignore                  Git 忽略规则
├── AGENTS.md                   仓库自动化操作规范
└── 工程目录结构说明.md          本文档
```

> 当前仓库的数据生成工具位于 `matlab/data_generator/`，没有单独的根级 `data_generator/` 目录。新工程应根据数据生成工具是否依赖 MATLAB，选择沿用当前布局或拆分为独立一级目录，但同一工程内应保持一致。

## 3. 各目录功能

### 3.1 `fpga/`

FPGA 开发内容的统一一级目录。Vivado RTL 设计、仿真、约束、IP、构建脚本以及 Vitis 软件均归入该目录，避免工具生成文件散落在仓库根目录。

### 3.2 `fpga/doc/`

存放 FPGA 工程相关的设计资料和说明文档，例如：

- 芯片、开发板和外设手册；
- 模块接口和通信协议；
- 系统框图与设计方案；
- 开发规范、调试记录和实施计划；
- 与硬件设计直接相关的说明性数据文件。

`fpga/doc/image/` 用于保存 Markdown 等文档引用的图片。长期有效的文档及其附件应纳入 Git 管理。

### 3.3 `fpga/scripts/`

存放能够自动创建、重建或验证工程的脚本，主要包括：

- 创建 Vivado 工程和 Block Design 的 Tcl 脚本；
- 生成或更新 IP 核的 Tcl 脚本；
- 调用 XSim 的仿真脚本；
- 模块综合、实现和报告生成脚本；
- 对生成结果进行辅助检查的 Python 脚本。

脚本应尽量使用相对于仓库的路径，并校验运行目录。调用 `vivado`、`xvlog`、`xelab` 或 `xsim` 时，应将 `fpga/work/` 设为当前工作目录。

### 3.4 `fpga/src/`

存放需要长期维护、纳入版本管理的 FPGA 设计输入。这里应保存“设计源”，而不是 Vivado 的临时工程和编译结果。

#### `fpga/src/hdl/`

存放正式设计使用的 Verilog、SystemVerilog、VHDL 源码，以及 `.vh` 等公共头文件。典型内容包括时钟、数据通路、控制状态机、接口适配器和顶层封装。

稳定的 Block Design wrapper 可以保存在此目录，但必须保证同一 Vivado 工程已通过 Tcl 脚本创建对应的 Block Design。wrapper 本身不能替代 Block Design 或其综合网表。

#### `fpga/src/sim/`

存放仅用于仿真的代码，包括：

- Testbench；
- 行为级仿真模型；
- 激励产生模块；
- 自动检查和结果比对模块。

这些文件通常加入 Vivado 的 simulation sources，不应误加入 synthesis sources。

#### `fpga/src/constrs/`

存放 XDC 约束文件，包括：

- FPGA 管脚与 I/O 电平标准；
- 输入、输出和生成时钟定义；
- 时序例外与跨时钟域约束；
- 面向特定顶层模块或开发板的约束。

建议按顶层设计或目标开发板拆分约束文件，避免不同硬件配置之间互相覆盖。

#### `fpga/src/ip/`

存放需要长期保存、供工程重建和复用的 IP 核内容，例如：

- `.xci`、`.xcix` 等 IP 配置文件；
- COE、MIF 等初始化文件；
- 自定义 IP 源码和约束；
- 工程重建所必需的生成源码或配套文件。

当前工程在该目录下保存了 DDS 和系数 BRAM IP。新工程可按 IP 功能建立独立子目录。

COE、MIF 等 IP 初始化文件统一放在对应的 `fpga/src/ip/` 子目录中，不在 `fpga/doc/` 中保留副本，以免出现多个版本并造成工程输入不一致。

Block Design 的 `.bd`、BD output products、自动生成 wrapper、OOC 构建缓存和运行目录不应从 Vivado 工程复制到这里；Block Design 的长期可重建来源应是 `fpga/scripts/` 下的 Tcl 脚本。

#### `fpga/src/testmodule/`

存放用于板级功能验证的顶层模块。这些模块通常把一个或多个待测功能连接到开发板的实际引脚，用于回环、数据通路或软硬件联调。

如果新工程规模较小，也可将此类模块并入 `fpga/src/hdl/`；若测试顶层较多，保留独立目录更便于区分正式系统顶层与阶段性测试设计。

### 3.5 `fpga/vitis/`

用于保存需要长期维护的 Vitis 内容，例如：

- PS/ARM 端应用源码；
- BSP 相关配置；
- 平台工程的可重建脚本与配置；
- 裸机程序或嵌入式软件公共组件。

当前目录可为空，表示已为后续 PS/PL 协同开发预留位置。Vitis 自动生成且可重新创建的缓存和编译产物不应提交到 Git。

### 3.6 `fpga/work/`

Vivado、Vitis 和 XSim 的本机工作区，存放：

- Vivado `.xpr` 工程；
- Block Design 及其 output products；
- `.runs/`、`.gen/`、`.srcs/`、`.cache/` 等生成目录；
- 综合、实现、bitstream 和导出产物；
- XSim 的 `xsim.dir/`；
- 日志、journal、报告和其他临时文件。

该目录整体被 `.gitignore` 忽略，可以删除后通过脚本重新生成。从远程仓库克隆项目后，该目录可能不存在，应手动创建 `fpga/work/`，再从该目录运行 `fpga/scripts/` 中的重建脚本。

禁止在仓库根目录创建另一个 `work/`，也不要在仓库根目录运行 Vivado 或 XSim 命令，否则日志和缓存会污染源码目录。

### 3.7 `matlab/`

存放系统级 MATLAB 代码，用于在 RTL 实现前后提供算法参考、输入数据和结果验证。

- `matlab/arm/`：ARM/PS 端算法的参考实现、参数打包和响应计算。
- `matlab/pl/`：PL 数据通路的数学参考模型，如 DDS、IQ 解调和数据累加。
- `matlab/models/`：ADC、DAC 和被测电路等系统组成部分的模型。
- `matlab/data_generator/`：生成滤波器系数、仿真激励和存储器初始化数据。
- `matlab/data_generator/data/`：保存生成的数据文件；是否提交 Git 应根据数据能否稳定重建及下游工程是否直接依赖它决定。
- `matlab/verification/`：将 MATLAB 参考结果与 RTL 仿真或实测数据进行比较。
- `matlab/results/`：存放运行结果和中间输出，通常仅保留占位 `.gitignore`，不提交批量生成结果。

`matlab/config.m` 可用于统一管理采样率、数据长度、文件路径等公共参数；`matlab/main.m` 可作为系统级仿真入口。

### 3.8 仓库根目录文件

- `.gitignore`：定义无需版本管理的本地工程与生成文件。当前主要忽略 `fpga/work/` 等构建产物。
- `AGENTS.md`：规定自动化工具在本仓库中的操作边界，包括目录布局、Vivado 运行位置和生成文件管理要求。
- `工程目录结构说明.md`：供开发者了解当前布局，并作为新工程建目录时的参考。

`.git/` 是 Git 自身维护的隐藏目录，不属于工程模板内容，也不应手动修改或复制到新工程。

## 4. 版本管理边界

| 内容 | 建议位置 | 是否纳入 Git |
| --- | --- | --- |
| RTL/HDL 源码 | `fpga/src/hdl/` | 是 |
| Testbench 和仿真模型 | `fpga/src/sim/` | 是 |
| XDC 约束 | `fpga/src/constrs/` | 是 |
| 可复用 IP 配置与必要文件 | `fpga/src/ip/` | 是 |
| 工程、BD 和 IP 重建 Tcl | `fpga/scripts/` | 是 |
| 设计文档和硬件手册 | `fpga/doc/` | 是 |
| COE/MIF 等 IP 初始化文件 | `fpga/src/ip/` | 是 |
| Vitis 应用源码与可重建配置 | `fpga/vitis/` | 是 |
| Vivado 工程与构建结果 | `fpga/work/` | 否 |
| BD `.bd` 和 output products | `fpga/work/` | 否，以 Tcl 作为长期来源 |
| XSim 缓存、日志和报告 | `fpga/work/` | 否 |
| MATLAB 源码和必要输入数据 | `matlab/` | 是 |
| MATLAB 批量结果和临时输出 | `matlab/results/` | 通常否 |

## 5. 新工程建议目录骨架

新工程可以先建立以下最小骨架，再按实际需求增加子目录：

```text
NewProject/
├── fpga/
│   ├── doc/
│   ├── scripts/
│   ├── src/
│   │   ├── constrs/
│   │   ├── hdl/
│   │   ├── ip/
│   │   └── sim/
│   ├── vitis/
│   └── work/               # Git 忽略
├── matlab/                 # 没有 MATLAB 工作流时可省略
│   ├── data_generator/
│   ├── models/
│   ├── results/            # Git 忽略生成结果
│   └── verification/
├── .gitignore
├── AGENTS.md
└── README.md
```

建议的初始化顺序为：

1. 建立目录骨架和 `.gitignore`。
2. 将 RTL、仿真和约束分别放入 `fpga/src/` 的对应子目录。
3. 在 `fpga/scripts/` 编写工程和 Block Design 重建脚本。
4. 在 `fpga/work/` 中运行 Vivado/XSim，并确认根目录没有生成日志、缓存或错误的 `work/`。
5. 删除 `fpga/work/` 后执行一次完整重建，以验证仓库中保存的内容足以恢复工程。

## 6. Vivado 工具使用约定

- 使用名为 `vivado2022` 的 Conda 环境，以及该环境配置的 `VIVADO_HOME`。
- 所有 Vivado、XSim 和相关验证命令均从 `fpga/work/` 目录运行。
- 所有自动生成的日志、缓存、报告和工程输出均留在 `fpga/work/`。
- 工程脚本应检查当前目录；若不是 `fpga/work/`，应立即报错退出。
- 完成构建或仿真后，应检查仓库根目录，确保不存在误建的根级 `work/`、`xsim.dir/`、日志或 journal 文件。
